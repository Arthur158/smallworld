
// // Send a raw WebSocket message
// export const sendWebSocketMessage = (message: any) => {
//   if (socket && socket.readyState === WebSocket.OPEN) {
//     socket.send(JSON.stringify(message));
//   } else {
//     console.error('WebSocket not open');
//   }
// };

// // Request-Response Function
// export const sendWebSocketRequest = (message: any): Promise<any> => {
//   if (!socket || socket.readyState !== WebSocket.OPEN) {
//     return Promise.reject(new Error('WebSocket not open'));
//   }
//
//   return new Promise((resolve, reject) => {
//     // Generate a unique ID for the request
//     const requestId = generateUniqueId();
//     message.id = requestId;
//
//     // Store the resolver function to handle the response later
//     pendingRequests[requestId] = resolve;
//
//     // Send the request
//     if(socket) {
//       socket.send(JSON.stringify(message));
//     } else {
//       console.error("websocket not initialized")
//     }
//
//     // Add a timeout for the request
//     setTimeout(() => {
//       if (pendingRequests[requestId]) {
//         reject(new Error(`Request timed out: ${requestId}`));
//         delete pendingRequests[requestId]; // Clean up the resolver
//       }
//     }, 5000); // Timeout after 5 seconds
//   });
// };
