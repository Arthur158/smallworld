# DriveEasily: Frontend
The frontend for booking a parking spot in the DriveEasily project. You can run the project using the following steps:
1. Navigate into the frontend folder.
2. Run `npm install` to install the dependencies.
3. Run `npm start` to start the frontend.

You can now access the frontend on `localhost:3000`.
