
alright so the idea would be:

in the goroutine where each player is being waited upon:
	the player would send a json with the type of the request, being:
		- request for a new tribe
		- request for a conquest
		- request for going into deployment
		- request for finishing turn
		- request for redeploying a character from piece stack to region and inversely
		- request for abandoning particular region
	the game state would have a "react" function, which would take in a json object and return another one. based on the "type" field, the server would know what to message to the players. 
	Okay maybe the react function is a bad idea and the game state could have a surface per type of request, with specific return types. For example, a conquest request would have as fields the user attacking, the type/index of its stack, and the id of the tile. The return would then be a error messgae for whether it succeeded or not, an updated piecestack list for the player (the whole list because, for example when using dragon, other stacks might get consumed in the process), and an updated piecestack for the attacked region. following this, the srever can do an if statement on the error message, and send all users the update in case it succeeded and send the attacker the error response in case it didnt.

Now we need to figure out the gamestate which is the hard part:

what fields should it have?
- turn
-list of players, their corresponding stacks, active and passive tribes (potentially regions owned, but maybe that can be calculated when needed, not sure which would be more efficient), their current cash reserve.
-the map as a list of tiles, each containing different stacks. Possibility of having an "owning race" field, or it could be traced from the type of the stacks. would be important when it comes to figuring out the price of the zone. the tiles adjacent to that tile. will need graphs algorithms to figure out distance between tiles. Because of that could be an idea to have a separate graph field which can be used to find distances and adjacency etc.
- the list of available tribes as well as the next coming ones. there should be a total list shuffled at the start of a game. there should also be the coins on all tiles. "Cursed" will be a problem in the future.

what types should there be?

player which is: a single active tribe, list of decline tribe (because of ancestral), their name ig (for knowing whose turn it is potentially and compare to whatever the server sends)
tribe, which has a race and a trait, trait just for display sometimes and race maybe to be able to discern who a tile belongs to, and then this is where the main beef is going to be: there should be a map of callback functions which are then going to be called at different stage of the game for deciding how an action happens. We'll have to make a list of all functions needed, but when being instantiated, the constructor of a tribe is going to need to look up the trait and race in a map containing bunch of callback

should tribe be a pointer or a field in struct Player?
	- when is it accessed? when a region gets conquered, the attacking tribe will be obtained through the player (some kind of function which will figure out if the stack is valid for the given player. This will be by calling some function)
	so player attacks, send the player that attacks and the type of pawn used and the id of the tile attacked. how to efficiently figure out if its valid? Check if its the player's turn, then check if the stack is valid for the players active tribe, if not, test it against the player's inactive tribes (for tribes like zombies). This subfunction should return an attacking tribe, whether it be passive or active. once that is obtained, one should obtain the attacked tribe. Since a tile has an owning player, launch a check similar to the one for finding the attacker

maybe have requests simply responded by an error message, and if none are given, then the game state requests the game state for the fields it needs
